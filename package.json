{
  "name": "jg.ar",
  "private": true,
  "devDependencies": {
    "@cloudflare/wrangler": "^1.19.8"
  },
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "publish": "npm run publish --workspaces --if-present",
    "wrangler": "wrangler"
  }
}
