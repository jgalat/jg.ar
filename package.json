{
  "name": "jg.ar",
  "private": true,
  "devDependencies": {
    "wrangler": "^2.6.2"
  },
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "publish": "npm run publish --workspaces --if-present",
    "wrangler": "wrangler"
  }
}
