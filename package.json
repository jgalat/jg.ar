{
  "name": "jg.ar",
  "private": true,
  "devDependencies": {
    "wrangler": "^2.16.0"
  },
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "publish": "npm run publish --workspaces --if-present",
    "wrangler": "wrangler"
  }
}
